
<div id="log">
  <h3>Activity Log</h3>
  <p>Please note that all entry deletions are permanant and will be wiped from your database!<p>

    <ul>
      <h3>Today</h3>
      <% @inputs_day.each do |input| %>
      <button><b>

        <%= link_to '<i class="fas fa-times delete"></i>'.html_safe,  { action: :destroy, id: input.id }, method: :delete, data: {confirm: "Are you sure? Your data will be deleted"} %>

        <%= input.goal.name %><br /></b><br /><%= input.created_at %>
      </button>
      <% end %>
    </ul>

    <ul>
      <h3>This Week</h3>
      <% @inputs_week.each do |input| %>
      <button><b>

        <%= link_to '<i class="fas fa-times delete"></i>'.html_safe,  { action: :destroy, id: input.id }, method: :delete, data: {confirm: "Are you sure? Your data will be deleted"} %>

        <%= input.goal.name %><br /></b><br /><%= input.created_at %>
      </button>
      <% end %>
    </ul>

    <ul>
      <h3>This Month</h3>
      <% @inputs_month.each do |input| %>
      <button><b>

        <%= link_to '<i class="fas fa-times delete"></i>'.html_safe,  { action: :destroy, id: input.id }, method: :delete, data: {confirm: "Are you sure? Your data will be deleted"} %>

        <%= input.goal.name %><br /></b><br /><%= input.created_at %>
      </button>
      <% end %>
    </ul>

    <ul>
      <h3>Complete List</h3>
      <% @inputs.each do |input| %>
      <button><b>

        <%= link_to '<i class="fas fa-times delete"></i>'.html_safe,  { action: :destroy, id: input.id }, method: :delete, data: {confirm: "Are you sure? Your data will be deleted"} %>

        <%= input.goal.name %><br /></b><br /><%= input.created_at %>
      </button>
      <% end %>
    </ul>


  </div>
